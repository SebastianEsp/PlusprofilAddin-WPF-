<Window
	x:Class="PlusprofilAddin.Views.ConnectorDialog"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:views="clr-namespace:PlusprofilAddin.Views"
	Title="Connector"
	mc:Ignorable="d" d:DataContext="{d:DesignInstance }">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<views:ConnectorUserControl Grid.Column="0" DataContext="{Binding SourceViewModel}" />
		<views:ConnectorUserControl Grid.Column="1" DataContext="{Binding TargetViewModel}" />
    <TextBox Grid.Column="2" Text="{Binding Text}"/>
	</Grid>
</Window>